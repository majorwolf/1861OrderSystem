# Database Configuration
PGUSER=orderapp
PGPASSWORD=publichouse1861
PGDATABASE=orderingdb
PGPORT=5432
PGHOST=postgres

# Application Configuration
NODE_ENV=production
PORT=5000
SESSION_SECRET=1861PublicHouseOrderingAppSecret

# Full database connection string
DATABASE_URL=postgresql://${PGUSER}:${PGPASSWORD}@${PGHOST}:${PGPORT}/${PGDATABASE}