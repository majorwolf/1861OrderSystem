# Development Dockerfile for 1861 Public House ordering system
# This is optimized for fast feedback loops during development

FROM node:20-slim

WORKDIR /app

# Install essential tools
RUN apt-get update && \
    apt-get install -y wget postgresql-client && \
    rm -rf /var/lib/apt/lists/*

# Copy package files
COPY package*.json ./

# Install all dependencies
RUN npm ci

# Copy the rest of the application
COPY . .

# Expose the port the app runs on
EXPOSE 5000

# Command to run the app in development mode
CMD ["npm", "run", "dev"]